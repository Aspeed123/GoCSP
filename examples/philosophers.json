{
  "nodes": [
    {
      "id": "hunger0",
      "type": "generator",
      "outputs": [ "out" ],
      "data": [ true, true, true ]
    },

    {
      "id": "hunger1",
      "type": "generator",
      "outputs": [ "out" ],
      "data": [ true, false, true ]
    },

    {
      "id": "fork0",
      "type": "generator",
      "outputs": [ "free" ],
      "data": [ true ]
    },

    {
      "id": "fork1",
      "type": "generator",
      "outputs": [ "free" ],
      "data": [ true ]
    },

    {
      "id": "philosopher0",
      "type": "processor",
      "inputs": [
        "hungry",
        "leftFree",
        "rightFree"
      ],
      "outputs": [ "eat" ],
      "operation": "hungry && leftFree && rightFree"
    },

    {
      "id": "philosopher1",
      "type": "processor",
      "inputs": [
        "hungry",
        "leftFree",
        "rightFree"
      ],
      "outputs": [ "eat" ],
      "operation": "hungry && leftFree && rightFree"
    },

    {
      "id": "printer",
      "type": "sink",
      "inputs": [ "in" ]
    }
  ],

  "edges": [
    {
      "from": "hunger0.out",
      "to": "philosopher0.hungry"
    },

    {
      "from": "fork0.free",
      "to": "philosopher0.leftFree"
    },

    {
      "from": "fork1.free",
      "to": "philosopher0.rightFree"
    },

    {
      "from": "hunger1.out",
      "to": "philosopher1.hungry"
    },

    {
      "from": "fork1.free",
      "to": "philosopher1.leftFree"
    },

    {
      "from": "fork0.free",
      "to": "philosopher1.rightFree"
    },

    {
      "from": "philosopher0.eat",
      "to": "printer.in"
    },

    {
      "from": "philosopher1.eat",
      "to": "printer.in"
    }
  ]
}